#include <iostream>
using namespace std;
int main()
{
	int row1;
	do {
		cout << "rows = ";
		cin >> row1;

	} while (row1 < 1);
	int col1;
	do {
		cout << "columns = ";
		cin >> col1;

	} while (col1 < 1);

	double** mat1 = new double*[row1];
	for (int i = 0; i < row1; ++i) { mat1[i] = new double[col1]; }
	double** mat2 = new double* [col1];
	for (int i = 0; i < col1; ++i) { mat2[i] = new double[row1]; }
	 
    //memset(row2, 0, row1 * sizeof(double));
	//memset(col2, 0, col1 * sizeof(double));

	for (int i = 0; i < row1; ++i) 
	{
		mat1[i] = 0;
		cout << mat1[i] << endl;
	}
	for (int i = 0; i < col1; ++i)
	{
	    mat2[i] = 0;
		cout << mat2[i] << endl;
	}

	for (int i = 0; i < row1; ++i) { delete[] mat1[i]; }
	for (int i = 0; i < row1; ++i) { delete[] mat1[i]; }
	delete[] mat1;
	delete[] mat2;
}
