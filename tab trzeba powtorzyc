#include <iostream>
using namespace std;
int main()
{
	int row1;
	do {
		cout << "rows = ";
		cin >> row1;

	} while (row1 < 1);
	int col1;
	do {
		cout << "columns = ";
		cin >> col1;

	} while (col1 < 1);

	double** row2 = new double*[row1];
	for (int i = 0; i < row1; ++i) { row2[i] = new double[col1]; }
	double** col2 = new double* [col1];
	for (int i = 0; i < col1; ++i) { col2[i] = new double[row1]; }
	 
    //memset(row2, 0, row1 * sizeof(double));
	//memset(col2, 0, col1 * sizeof(double));

	for (int i = 0; i < row1; ++i) 
	{
		row2[i] = 0;
		cout << row2[i] << endl;
	}
	for (int i = 0; i < col1; ++i)
	{
	    col2[i] = 0;
		cout << col2[i] << endl;
	}

	for (int i = 0; i < row1; ++i) { delete[] row2[i]; }
	for (int i = 0; i < row1; ++i) { delete[] row2[i]; }
	delete[] row2;
	delete[] col2;
}
